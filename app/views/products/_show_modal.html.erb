<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal" aria-label="Close">
    <span aria-hidden="true">×</span></button>
  <h4 class="modal-title text-capitalize"><%= product.name %></h4>
</div>
<div class="modal-body" style="">
  <div class="row">
    <div class="col-md-12" style="">
      <img src="<%= product.image.url(:medium) %>" data-holder-rendered="true" class="thumbnail col-xs-12 col-sm-6">
      <div class="col-sm-6">
        <h4><strong>Description:</strong></h4>
        <p><%= product.description %></p>
      </div>
      <div class="col-sm-6">
        <h4><strong>Restaurant:</strong></h4>
        <p><%= product.restaurant.name %></p>
      </div>
    </div>
  </div>

  <span class="price label-warning">$ <%= product.price %></span>

  <p id="notice"><%= notice %></p>
</div>
<div class="modal-footer">
  <%= link_to '<i class="fa fa-cart-plus" aria-hidden="true"></i> Add to Cart'.html_safe, 'javascript:;',
              class: 'btn btn-flat bg-purple add-product-effect add-product', data: {id: product.id} %>
</div>
